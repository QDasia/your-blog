```sh
# 安装全局依赖

$ npm install -g hexo-cli

# 安装本地依赖

$ npm install

# 安装主题

$ mkdir themes && cd themes

# 使用hexo-theme-next主题
$ git clone https://github.com/iissnan/hexo-theme-next.git

# 开启服务

$ cd ..

$ hexo serve

# 生成文件

$ hexo generate
```